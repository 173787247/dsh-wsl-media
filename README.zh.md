# dsh-wsl-media

WSL 本地媒体工具：`media_probe`（ffprobe）、`media_pdf_text`、`media_asr`（需 PATH 上有 whisper）。

路径限制在家目录 / `~/.dsh` / `/tmp` / `/mnt/c|d`。

```sh
dsh plugin --profile web add github:173787247/dsh-wsl-media
```

## License

MIT
